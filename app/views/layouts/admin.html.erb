<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Prowl Admin</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YcnS/1AaQUS23mLOE9T7YL5l0pOqL1jHJ6ZS" crossorigin="anonymous">
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>
  <style>
    body { min-height: 100vh; }
    .sidebar {
      width: 240px;
      min-height: 100vh;
      background-color: #212529;
    }
    .sidebar .nav-link {
      color: rgba(255,255,255,.75);
      padding: 0.6rem 1rem;
      border-radius: 0.25rem;
      margin: 0.1rem 0.5rem;
    }
    .sidebar .nav-link:hover,
    .sidebar .nav-link.active {
      color: #fff;
      background-color: rgba(255,255,255,.1);
    }
    .sidebar .nav-link.active {
      background-color: rgba(13,110,253,.35);
    }
    .sidebar-brand {
      color: #fff;
      font-size: 1.25rem;
      font-weight: 600;
      padding: 1rem;
      border-bottom: 1px solid rgba(255,255,255,.1);
    }
    .sidebar-section {
      color: rgba(255,255,255,.4);
      font-size: 0.75rem;
      text-transform: uppercase;
      letter-spacing: 0.05em;
      padding: 1rem 1rem 0.25rem;
    }
    .main-content {
      flex: 1;
      min-width: 0;
    }
  </style>
</head>
<body>
  <div class="d-flex">
    <!-- Sidebar -->
    <nav class="sidebar d-flex flex-column flex-shrink-0">
      <div class="sidebar-brand">
        Prowl Admin
      </div>

      <div class="sidebar-section">Navigation</div>
      <ul class="nav flex-column">
        <li class="nav-item">
          <a class="nav-link <%= 'active' if controller_name == 'dashboard' %>" href="<%= admin_root_path %>">
            Dashboard
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= 'active' if controller_name == 'shops' %>" href="<%= admin_shops_path %>">
            Shops
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= 'active' if controller_name == 'scans' %>" href="<%= admin_scans_path %>">
            Scans
          </a>
        </li>
        <li class="nav-item">
          <a class="nav-link <%= 'active' if controller_name == 'issues' %>" href="<%= admin_issues_path %>">
            Issues
          </a>
        </li>
      </ul>

      <div class="mt-auto p-3 border-top border-secondary">
        <div class="text-white-50 small mb-1"><%= current_admin.email %></div>
        <%= link_to "Logout", destroy_admin_session_path, data: { turbo_method: :delete }, class: "btn btn-outline-light btn-sm w-100" %>
      </div>
    </nav>

    <!-- Main content -->
    <div class="main-content">
      <div class="p-4">
        <% if notice.present? %>
          <div class="alert alert-success alert-dismissible fade show" role="alert">
            <%= notice %>
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
          </div>
        <% end %>
        <% if alert.present? %>
          <div class="alert alert-danger alert-dismissible fade show" role="alert">
            <%= alert %>
            <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
          </div>
        <% end %>

        <%= yield %>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz" crossorigin="anonymous"></script>
</body>
</html>
