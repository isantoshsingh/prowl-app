<%# Prowl - App Home Page %>
<%# Shows onboarding setup guide for new merchants, or the active dashboard %>

<% if @show_onboarding %>
  <%= render "home/onboarding" %>
<% else %>
  <%= render "home/dashboard" %>
<% end %>
