{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // .tool_response.filePath' | { read -r f; if echo \"$f\" | grep -qE '\\.rb$'; then bin/rubocop --force-exclusion \"$f\" 2>/dev/null || true; fi; }",
            "timeout": 30,
            "statusMessage": "Running RuboCop..."
          }
        ]
      }
    ]
  }
}
